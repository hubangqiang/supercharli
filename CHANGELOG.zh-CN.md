# 变更日志

本文件记录本项目所有重要变更。

格式参考 Keep a Changelog。
本项目遵循语义化版本（Semantic Versioning）。

## [Unreleased]

### 新增
- 治理文档与分阶段实施计划。
- 基于 SQLite 的本地记忆引擎，支持 L1/L2 跨重启持久化。
- 支持会话级升级与 TTL 回落的严重性状态机。
- 覆盖回退、人格再生成、记忆持久化与状态迁移的运行时测试。
- 结构化路由元数据与多级回退链（重试 -> 次级 -> 最小安全回复）。
- 轻量 Telemetry 组件（阶段日志与运行计数）。
- 带清单校验的快照备份与恢复脚本。
- 模型无关、文件驱动的 Provider 适配层。
- 覆盖 Provider 解析、注册表行为、路由回退的验收测试。
- macOS 开机环境加载脚本与模型配置模板。
- 后台守护进程与交互式 CLI。
- 仓库外运行包装脚本（外部状态目录）。
- 引导式用户初始化命令（角色/背景/人格配置）。
- 默认运行时与记忆路径统一在仓库外（`~/supercharli-runtime`）。
