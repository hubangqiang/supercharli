# 当前状态快照

## 项目阶段
- 当前处于：V1 规划完成并进入实现起步阶段。
- 实现状态：已落地运行时核心骨架，完成本地可持久化记忆、风险状态机、多级回退链、基础可观测、快照迁移、provider 适配层与后台交互 CLI。

## 已完成
- 产品原则文档体系已建立并分层。
- 工程原则文档已建立。
- V1 计划文档（范围、验收、记忆、路由、可观测、迁移）已形成。
- 开源协作治理文件（贡献、行为准则、安全、支持、模板）已建立。
- 运行时代码骨架已建立（Kernel、Persona Guard、Memory Engine、Router、Fallback、Demo Runner）。
- SQLite 本地记忆引擎已接入，L1/L2 可跨重启保留。
- 严重性状态机已接入，支持会话级升级与 TTL 回落。
- 路由输出已包含 route reason / fallback level / response mode。
- 回退策略已实现：主模型重试一次 -> 次级模型 -> 最小安全回复。
- 轻量 Telemetry 已接入，覆盖生命周期阶段日志与核心计数。
- 快照备份/恢复脚本已接入，支持清单校验后恢复。
- Provider 适配层已接入，采用外部配置驱动，不在仓库内固化具体模型信息。
- 后台 daemon + CLI 已接入，支持会话级持续交互。
- 新用户引导初始化已接入，可生成专属人格/角色/背景配置并立即使用。
- 基础回归测试已覆盖核心流、回退链、记忆晋升、持久化、状态机、快照、provider 与 daemon。

## 已采用流程
- 分支开发优先：`codex/*`。
- 阶段完成后再合并至 `main`。
