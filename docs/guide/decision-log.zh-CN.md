# 关键决策日志

## 已确认决策
- 采用双轨治理：产品原则与工程原则分离。
- V1 仅做对话与记忆，不做外部执行动作。
- 记忆采用 L1/L2 机制，关键重复模式可自动晋升 L2。
- 严重事件采用 S1/S2/S3 临时偏离机制并回归常态。
- 模型可替换，但身份与记忆主权必须保持本地一致。
- 采用分支开发，阶段完成后再合并 `main`。

## 变更规则
- 新决策必须追加，不覆盖历史记录。
- 每条决策应说明：背景、影响范围、是否影响验收标准。
