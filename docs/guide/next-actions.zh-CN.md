# 下一步动作清单

## P1（优先）
- 将 `docs/plans/*` 从规划稿细化为可执行实现任务。
- 定义最小运行路径（输入 -> 记忆 -> 路由 -> 输出）。
- 建立最小验证脚本与验收样例。

## P2
- 明确 L1/L2 的运行时策略与回收机制实现计划。
- 明确 S1/S2/S3 触发信号和回归机制实现计划。

## P3
- 确立多模型一致性测试集。
- 完成模型切换与回退链验证方案。

## 合并门禁
- 通过当前阶段验收标准后才允许合并到 `main`。
